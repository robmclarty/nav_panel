require 'rails/engine'

module AdminNav
  module Rails
    # Although empty, this will cause rails to add its directories to the load path when the gem is required.
    class Engine < ::Rails::Engine
    end
  end
end